node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
.DS_Store
.idea/

# Credentials. The server also writes a config file under the user's XDG
# config dir; nothing secret should ever land inside the repo.
.env
.env.local
*.key

# Network captures. A HAR from a logged-in session carries cookies, bearer
# tokens and full account payloads — everything an attacker would need. These
# are working files for building an adapter and must never be committed.
tmp/
*.har
